addEventListener("click, startGame");

function startGame() {

    if button.addEventListener("click", startGame) {
        startGame() = true;
    }

    var words = ["Cheese", "Donuts", "Spidermuffins", "Hypens", "Colons", "Spaghetti", "Eyeballs", "Hellcatch Valley", "Boredom", "Agony"];
    
    var guessWord = words[Math.floor(Math.random()*words.length)];

    var guessedWord = "";
}


